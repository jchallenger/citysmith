# Built from the user's own TaleSpire install -- not portable, and not ours to
# redistribute. Both regenerate locally in seconds:
#   catalog.json        python -m citysmith catalog build
#   docs/asset-index.md python tools/kit_index.py
catalog.json
docs/asset-index.md

out/
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
build/
dist/

# Community slabs downloaded for study (tools/harvest_slabs.py regenerates them).
# These are other builders' work from Tales Tavern -- we learn construction
# technique from them, we do not redistribute their geometry.
library/
.claude/worktrees/
